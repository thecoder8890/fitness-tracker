<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ws.design.fitness_track">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Splash_ftActivity" />
        <activity android:name=".Home_ftActivity" />
        <activity android:name=".Workoutdetail_ftActivity" />
        <activity android:name=".Workout_started_ftActivity" />
        <activity android:name=".Mygoal_ftActivity" >

        </activity>
        <activity android:name=".List_recycleActivity" />
        <activity android:name=".Splace_screenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Report_ftActivity" >

        </activity>
        <activity android:name=".Settings_ftActivity">

        </activity>
    </application>

</manifest>